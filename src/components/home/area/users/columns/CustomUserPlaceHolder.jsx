import React from "react"
import {Form} from "react-bootstrap"
const CustomUserPlaceHolder = ({child})=>{
    return(
        <div className={`area-user-column ${child}`}>

        </div>
    )
}

export default CustomUserPlaceHolder