import React from "react"
import {Form} from "react-bootstrap"
const CustomUserDateColumn = ({child})=>{
    return(
        <div className={`area-user-column ${child}`}>

        </div>
    )
}

export default CustomUserDateColumn